package com.stackroute.nlpservice.service;
//create a enum for taking time unit
public enum DurationTime {
    SEC,
    MIN,
    SECOND,
    SECONDS,
    MINUTE,
    MINUTES,
    HR,
    HOUR,
    HOURS
}
